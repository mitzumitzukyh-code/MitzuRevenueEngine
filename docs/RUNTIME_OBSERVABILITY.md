# Runtime observability

Scout and market workers emit scan summaries and errors to Railway runtime logs while continuing to persist activity in PostgreSQL. Wallet and autonomous execution remain disabled.
