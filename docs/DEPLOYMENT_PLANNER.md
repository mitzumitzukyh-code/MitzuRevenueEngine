# Deployment Planner

Creates staging plans only. Deployment remains blocked behind CI, evaluation, zero-payment, zero-spend, and explicit approval gates. Wallet and x402 are disabled and the staging budget cap is $0.
